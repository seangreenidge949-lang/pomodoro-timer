# 番茄时钟 v3 — 产品需求文档

## 1. 背景与问题

v2 已交付 Web（单文件 vanilla HTML）和 macOS（SwiftUI）双平台极简番茄钟，核心功能完整：25 分钟倒计时 → 全屏覆盖提醒 → 音效通知 → 按键退出。

v2 解决了"有一个能用的计时器"的问题，但用户在实际使用中暴露出三类不足：

| 问题类别 | 具体表现 |
|----------|---------|
| **感知断裂** | 切到其他窗口后无法感知计时状态，必须切回页面确认"还剩多久" |
| **反馈单调** | 计时过程只有数字跳动，缺乏"时间在流逝"的直觉；提醒音效偏冷（纯正弦波） |
| **灵活性不足** | 固定 25 分钟无法适配不同任务类型（如 15 分钟短任务、45 分钟深度工作） |

v3 在不破坏极简定位的前提下，针对性补齐这三类不足。

## 2. 价值论述

采用 **Kano 模型** 分类 8 个候选功能：

| 分类 | 功能 | 价值逻辑 |
|------|------|---------|
| **必备型（Must-be）** | F-05 标题计时 | 后台可见性是计时器的基本预期，缺失即扣分 |
| **期望型（One-dimensional）** | F-03 进度感知、F-06 可调时长 | 做了加分，不做不扣分，但直接提升使用频次 |
| **魅力型（Attractive）** | F-01 呼吸动效、F-04 覆盖层强化、F-02 音效升级 | 超出预期的精致感，用户不会主动要求但体验后印象深刻 |
| **差异型（Differentiator）** | F-08 今日计数器、F-10 专注模式联动 | 从工具到习惯的跨越——计数器提供正反馈，专注模式将番茄钟从"应用内"延伸到"系统级" |

**优先级排序原则**：必备型先行 → 期望型跟进 → 魅力型穿插（低成本高感知的先做）→ 差异型收尾。

## 3. 目标

| 目标 | 验收方式 |
|------|---------|
| 后台状态可感知 | Web 标签页标题实时显示倒计时；macOS Dock badge 显示剩余分钟 |
| 计时过程有进度直觉 | 进度条 + 呼吸动效让用户不看数字也能感知"快到了" |
| 提醒体验更有仪式感 | 覆盖层动效 + 升级音效，before/after 录屏对比可感知差异 |
| 支持不同时长的任务 | 可选 15/20/25/30/45/60 分钟，持久化存储 |
| 建立每日使用习惯 | 今日计数器提供完成感反馈 |
| macOS 系统级专注 | 计时自动启用系统专注模式（macOS 独占） |

## 4. 目标用户与场景

**用户画像**：需要管理专注时间的知识工作者，偏好极简工具，排斥功能臃肿的生产力 App。

**场景 1：后台计时**
> 用户启动番茄钟后切到 IDE 写代码。浏览器标签页标题显示 `18:32 - Pomodoro`，余光一扫就知道还剩多久。macOS 用户看 Dock 图标上的 `18` badge。

**场景 2：深度工作调时长**
> 用户要进入 45 分钟的深度阅读。在空闲状态长按计时数字，弹出时长选项，选择 45 分钟。下次打开自动保持 45 分钟。

**场景 3：一天的节奏感**
> 上午完成 3 个番茄后，界面底部显示 `● ● ● ○`。还差 1 个完成上午目标，产生"再来一个"的动力。

**场景 4：零干扰专注（macOS）**
> 用户点击 START，系统自动进入专注模式，通知全部静音。25 分钟后提醒弹出，关闭提醒后系统恢复原状态。

## 5. 设计原则

| 原则 | 含义 | 违反示例 |
|------|------|---------|
| **极简不增复杂** | 新功能不增加可见 UI 元素（进度条 2px、圆点 10px、呼吸动效无 UI）或隐藏在手势后 | 加设置页面、加菜单栏 |
| **渐进增强** | 每个功能独立，关闭/缺失不影响核心计时 | 计数器挂了导致计时中断 |
| **双平台一致体验** | 功能行为一致，实现方式适配平台特性 | Web 用 Notification API 而 macOS 用 Dock badge |
| **默认最优** | 所有新功能开箱即用，无需配置 | 需要用户手动开启进度条 |

## 6. 方案

### F-05 页面标题实时计时

**功能描述**：浏览器标签页标题实时显示倒计时，macOS 在 Dock 图标显示剩余分钟 badge。

**平台差异**：

| 维度 | Web | macOS |
|------|-----|-------|
| 显示位置 | 浏览器标签页标题 | Dock 图标 badge |
| 计时中 | `MM:SS - Pomodoro` | 剩余分钟数（如 `18`） |
| 提醒中 | `时间到！` | `!` |
| 空闲/退出 | `Pomodoro Timer` | badge 清空 |

**设计规格**：
- Web 标题格式：`{MM}:{SS} - Pomodoro`，每秒更新，与 `updateDisplay()` 同步
- macOS badge：纯数字字符串，`NSDockTile.badgeLabel`

**交互逻辑**：
- 状态 `running` / `paused`：显示倒计时
- 状态 `alert`：显示提醒标记
- 状态 `idle`：清空 / 恢复默认

**状态条件**：无开关，始终生效。

**验收标准**：
- [ ] Web: 计时中标签页标题显示 `MM:SS - Pomodoro` 且每秒更新
- [ ] Web: 提醒时标题变为 `时间到！`
- [ ] Web: 退出提醒后标题恢复 `Pomodoro Timer`
- [ ] macOS: 计时中 Dock badge 显示剩余分钟数
- [ ] macOS: 提醒时 Dock badge 显示 `!`
- [ ] macOS: 空闲状态 Dock badge 清空

---

### F-03 进度感知

**功能描述**：计时数字下方增加一条极细进度条，从左到右线性填充。

**平台差异**：

| 维度 | Web | macOS |
|------|-----|-------|
| 实现 | `<div>` + CSS width 百分比 | `GeometryReader` + `Rectangle` |
| 位置 | `#timerDisplay` 与 `#actionBtn` 之间 | `Text(timeString)` 下方 |
| 显隐 | opacity 控制 | `.opacity()` 修饰符 |

**设计规格**：
- 高度：2px
- 圆角：1px
- 宽度：与计时数字等宽（约 320px Web / 匹配 `GeometryReader` 宽度 macOS）
- 背景色轨道：`#E0E0E0`
- 填充色：`#000000`
- 进度公式：`(DURATION - remaining) / DURATION * 100%`
- 动画：无过渡动画（每 200ms 刷新一次宽度，视觉上已足够平滑）

**交互逻辑**：
- `running`：可见，持续填充
- `paused`：可见，填充停在当前位置
- `idle` / `alert`：不可见（opacity: 0）

**验收标准**：
- [ ] 仅在 running 和 paused 状态可见
- [ ] 从 0% 到 100% 线性填充，与倒计时同步
- [ ] 高度 ≤ 3px，宽度与计时数字等宽或略窄
- [ ] 空闲和提醒状态下不可见
- [ ] 不使用亮色、不闪烁

---

### F-01 呼吸动效

**功能描述**：计时运行中，倒计时数字透明度做 4 秒周期缓动循环。

**平台差异**：

| 维度 | Web | macOS |
|------|-----|-------|
| 实现 | CSS `@keyframes` + class toggle | SwiftUI `.opacity()` + `withAnimation` |
| 触发 | `classList.add('breathing')` | `.onChange(of: state)` |

**设计规格**：
- 透明度范围：0.88 – 1.0（12% 变化幅度）
- 周期：4 秒（2 秒渐暗 + 2 秒渐亮）
- 缓动曲线：`ease-in-out`
- CSS keyframes：`0%,100% { opacity: 1 } 50% { opacity: 0.88 }`

**交互逻辑**：
- `running`：动效运行
- `paused` / `idle` / `alert`：动效停止，opacity 固定为 1.0

**验收标准**：
- [ ] 仅 running 状态有呼吸效果
- [ ] 暂停时呼吸停止，数字完全不透明
- [ ] 周期约 4 秒，肉眼可感知但不分心
- [ ] 空闲状态无动效

---

### F-08 今日计数器

**功能描述**：界面底部用极淡圆点显示今日已完成的番茄数。默认目标 4 个。跨天自动归零。

**平台差异**：

| 维度 | Web | macOS |
|------|-----|-------|
| 存储 | `localStorage` JSON `{date, count}` | `UserDefaults` |
| 渲染 | DOM `<div>` 圆点 | SwiftUI `HStack` + `Circle` |
| 触发 +1 | `dismissAlert()` 末尾 | `TimerManager.reset()` 后回调 |

**设计规格**：
- 圆点直径：8px
- 圆点间距：8px
- 实心（已完成）：`#CCCCCC`
- 空心（未完成）：`#E8E8E8`，1px stroke
- 与按钮间距：≥ 40px
- 目标数默认：4
- 超过目标：继续显示实心圆点，不封顶，不再显示空心占位

**交互逻辑**：
- 每次关闭提醒（dismiss）→ count + 1
- 页面/应用加载 → 读取存储，校验日期（跨天则 count = 0）
- 空闲状态：今日有完成记录则显示圆点，否则不显示容器
- running / paused / alert：圆点保持显示（如有记录）

**存储格式**（Web）：
```json
{ "date": "2026-03-10", "count": 3 }
```

**验收标准**：
- [ ] 关闭提醒后计数 +1
- [ ] 页面/应用重新打开后计数保持（同一天）
- [ ] 跨天后计数自动归零
- [ ] 圆点颜色极淡，不干扰主视觉
- [ ] 无完成记录时不显示

---

### F-04 提醒覆盖层强化

**功能描述**：三层增强——背景色呼吸、文字缩放脉动、关闭淡出过渡。

**平台差异**：

| 维度 | Web | macOS |
|------|-----|-------|
| 背景呼吸 | CSS `@keyframes` 颜色交替 | SwiftUI `.animation` 颜色交替 |
| 文字脉动 | CSS `@keyframes` scale | SwiftUI `.scaleEffect` |
| 关闭淡出 | class `dismissing` + `transitionend` | `withAnimation(.easeOut)` + completion |

**设计规格**：

*背景色呼吸*：
- 颜色 A：`#E74C3C`（当前红）
- 颜色 B：`#C0392B`（深红）
- 周期：3 秒
- 缓动：`ease-in-out`

*文字缩放脉动*：
- scale 范围：1.0 – 1.02（2% 变化）
- 周期：2 秒
- 缓动：`ease-in-out`
- 作用于：`.overlay-title`（"时间到！"文字）

*关闭淡出*：
- 动画：`transform: scale(0.95)` + `opacity: 0`
- 时长：300ms
- 缓动：`ease-out`
- 流程（Web）：点击/按键 → 添加 `dismissing` class → 等待 `transitionend` → 添加 `hidden` + 执行 reset 逻辑

**交互逻辑**：
- 覆盖层出现时：背景呼吸 + 文字脉动立即开始
- 用户按键/点击：触发淡出过渡
- 过渡完成后：隐藏覆盖层，恢复 idle 状态
- macOS 多屏：所有屏幕的 overlay 动效同步

**验收标准**：
- [ ] 提醒背景有缓慢明暗呼吸效果
- [ ] "时间到！"文字有微弱缩放脉动
- [ ] 关闭时有 ~300ms 缩小淡出，非瞬间消失
- [ ] macOS 多显示器 overlay 动效同步

---

### F-06 可调时长

**功能描述**：长按计时数字进入编辑模式，选择预设时长。仅 idle 状态可触发。

**平台差异**：

| 维度 | Web | macOS |
|------|-----|-------|
| 触发手势 | `mousedown`/`touchstart` 500ms | `.onLongPressGesture(minimumDuration: 0.5)` |
| 选择 UI | 数字下方横排预设选项 | SwiftUI overlay / Picker |
| 存储 | `localStorage` | `UserDefaults` |
| 架构影响 | `DURATION` 从 const 改为 let 变量 | `defaultDuration` 从 `static let` 改为 `@Published var` |

**设计规格**：
- 预设选项：15 / 20 / 25 / 30 / 45 / 60（分钟）
- 默认选中：25 分钟
- 选中标记：加粗 + 下划线（Web）/ 高亮背景（macOS）
- 选项字号：16px / system body
- 选项颜色：`#666666`，选中为 `#000000`
- 选项间距：16px
- 编辑模式下隐藏 START 按钮，点选后退出编辑模式

**交互逻辑**：
- 空闲状态 + 长按 ≥ 500ms → 进入编辑模式
- 编辑模式中：显示预设选项，隐藏按钮
- 点选某个选项 → 更新 duration + 持久化 + 退出编辑模式 + 更新显示
- running / paused / alert 状态：长按无效
- 点击编辑模式外区域 → 退出编辑模式不保存

**跨功能影响**：
- F-03 进度条：进度公式中的 `DURATION` 需改为动态值
- F-08 今日计数器：不受时长影响——无论几分钟，完成一个就算一个番茄

**验收标准**：
- [ ] 空闲状态长按 ≥ 500ms 进入编辑模式
- [ ] 显示 6 个预设时长选项
- [ ] 选择后倒计时数字立即更新
- [ ] 选择持久化，重启后保持
- [ ] running/paused/alert 状态长按无效
- [ ] 当前选中时长有视觉标记

---

### F-02 音效升级

**功能描述**：泛音叠加替代纯正弦波，渐强音量序列，ADSR 包络消除 click 噪音。

**平台差异**：

| 维度 | Web | macOS |
|------|-----|-------|
| API | Web Audio API `OscillatorNode` | AVAudioEngine `AVAudioPCMBuffer` |
| 函数 | `playAlertSound()` | `AudioManager.generateChimeBuffer()` |
| 调音 | 浏览器内实时合成 | 预生成 buffer 播放 |

**设计规格**：

*音色*：
- 每个音符 3 个振荡器：基频 f + 2次谐波 2f + 3次谐波 3f
- 振幅比：1.0 : 0.3 : 0.1
- 效果：接近木琴/钟声质感

*音量序列*：
- 4 音渐强：`[0.15, 0.2, 0.3, 0.35]`
- 音符间隔：600ms

*ADSR 包络*：
- Attack: 10ms（线性上升）
- Decay: 无
- Sustain: 保持峰值
- Release: 80ms（指数衰减至 0.001）

**注意事项**：
- Web Audio API 和 AVAudioEngine 渲染差异，同参数听感可能不同，需分别调音
- 建议录屏对比 before/after
- 不做"最后 1 分钟滴答"——争议大，单独评估

**验收标准**：
- [ ] 提醒音效比当前更温暖（非纯正弦波）
- [ ] 无 click/pop 噪音
- [ ] 4 声序列有明显轻→重渐进感
- [ ] Web 和 macOS 听感接近（不要求完全一致）
- [ ] 音量适中，不刺耳

---

### F-10 专注模式联动（macOS 独占）

**功能描述**：计时开始自动启用系统专注模式，结束/取消后恢复原状态。

**平台差异**：

| 维度 | Web | macOS |
|------|-----|-------|
| 实现 | 不实现 | 新建 `FocusManager.swift` |

**技术路径（需 spike 验证）**：

| 路径 | 方式 | 优劣 |
|------|------|------|
| ① NSUserDefaults | 直写 DND 状态 | ❌ 已被 Apple 废弃 |
| ② Shortcuts 框架 | `INSetDNDShortcut` | 需 entitlement，审核风险 |
| ③ AppleScript | System Events 桥接 | 需辅助功能权限，用户感知差 |
| ④ shortcuts CLI | 调用预设快捷指令 | ✅ 最轻量，但需用户预配置 |

**推荐路径**：先 spike 验证路径 ④，如不可行依次尝试 ③ → ②。

**交互逻辑**：
- 点击 START → 检查当前专注模式状态 → 如未开启则自动启用
- 提醒关闭 / 手动 reset → 恢复启动前的专注模式状态
- 用户已手动开启专注模式 → 不做重复操作，结束时也不关闭
- 权限不足 → 静默失败，不阻塞核心计时功能

**降级方案**：如果所有 API 路径受限过多，降级为"提供指引让用户手动配置 Shortcuts 自动化"。

**验收标准**：
- [ ] START 后系统专注模式自动启用
- [ ] 提醒关闭后恢复原状态
- [ ] 手动 reset 后同样恢复
- [ ] 用户已开启时不重复操作
- [ ] 权限不足时静默失败

## 7. 跨功能关注点

| 关注点 | 涉及功能 | 说明 |
|--------|---------|------|
| DURATION 动态化 | F-06 ↔ F-03, F-05 | F-06 将 DURATION 从常量改为变量后，进度条和标题计时必须使用动态值 |
| 今日计数不区分时长 | F-06 ↔ F-08 | 无论 15 分钟还是 60 分钟，完成一个就是一个番茄 |
| Web 单文件约束 | 全部 Web 功能 | 所有功能内联在 index.html，不拆分文件 |
| macOS 新增文件注册 | F-08, F-10 | `SessionCounter.swift`、`FocusManager.swift` 需在 Package.swift 注册 |
| 多屏同步 | F-04 | macOS 覆盖层动效需所有显示器同步 |

## 8. 范围边界

**本期包含**：

- 8 个功能，分 2 批实现（Batch 1: F-05/F-03/F-01/F-08/F-04；Batch 2: F-06/F-02/F-10）
- 双平台同步实现（F-10 除外）

**本期不包含**：

| 排除项 | 原因 |
|--------|------|
| 休息倒计时 / 自动循环 | 增加状态复杂度，与极简定位冲突 |
| 历史记录 / 统计图表 | 今日计数器足够，历史回溯不是 MVP |
| 通知 API（Web） | 页面标题 + 全屏覆盖已足够，通知权限体验差 |
| 暗色 / 亮色主题切换 | 优先级低，可后续独立迭代 |
| 最后 1 分钟滴答音效 | 争议大——可能干扰专注，需单独用户调研 |
| 自定义目标数（计数器） | 默认 4 足够，后续可与可调时长联动 |
| Web 版专注模式 | 浏览器无系统级控制能力 |

## 9. 依赖与风险

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| F-10 API 路径全部受限 | 专注模式联动无法实现 | 降级为用户指引文档 |
| F-06 架构变更引入回归 | DURATION 动态化影响进度条、标题计时 | Batch 2 先做 F-06，做完后回归验证 F-03/F-05 |
| Web Audio API 跨浏览器差异 | F-02 音效在 Safari/Firefox 表现不一致 | 调音阶段在 Chrome + Safari 双验证 |
| macOS 多屏覆盖层动效性能 | F-04 动效可能在低端 Mac 掉帧 | 动效参数保守（3s/2s 周期），不做高频动画 |

## 10. 验收标准（全局）

### Batch 1 验收（预估 2-3 小时）

- [ ] F-05: Web 标签页标题实时更新；macOS Dock badge 正确显示
- [ ] F-03: 进度条仅 running/paused 可见，线性填充与倒计时同步
- [ ] F-01: 呼吸动效仅 running 生效，暂停时停止
- [ ] F-08: 今日计数正确持久化，跨天归零
- [ ] F-04: 覆盖层三层动效运行，关闭有 300ms 淡出

### Batch 2 验收（预估 4-8 小时）

- [ ] F-06: 长按编辑模式正常，时长持久化，进度条/标题计时使用动态值
- [ ] F-02: 音效温暖无噪音，渐强序列明显
- [ ] F-10: 专注模式自动启用/恢复（或降级方案文档就绪）

### 回归验证

- [ ] v2 所有验收标准仍通过
- [ ] 双击 index.html 可在浏览器正常打开
- [ ] macOS 应用编译运行无 warning
