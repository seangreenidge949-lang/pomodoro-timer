# 番茄时钟 v2 — 产品需求文档

## 背景

需要一个零干扰的番茄工作法计时工具。现有版本（React SPA）过于复杂，启动慢、维护成本高。此次从零重做，追求极简。

## 目标

- 提供 25 分钟专注倒计时
- 时间到时通过**全屏覆盖 + 音效**确保用户注意到提醒
- 整个应用为单 HTML 文件，无需构建工具，双击即可打开

## 核心用户场景

> 用户正在电脑前工作，打开番茄时钟网页，点击"开始"按钮。屏幕角落显示倒计时。25 分钟后，整个浏览器页面被纯色覆盖层遮住，大字提示"时间到！"并播放提示音。用户按任意键退出提醒，回到初始状态。

## 功能方案

### 页面结构

两个状态，同一个页面：

**状态 1：计时界面**
- 居中显示倒计时 `MM:SS`
- 下方一个按钮：开始 / 暂停 / 重置
- 极简设计，无多余元素

**状态 2：全屏提醒**
- `position: fixed` 覆盖层，覆盖整个视口
- 纯色背景（如番茄红 #E74C3C）
- 居中大字："时间到！休息一下"
- 小字提示："按任意键返回"
- 播放提示音（Web Audio API 生成，无需外部文件）

### 交互逻辑

| 操作 | 行为 |
|------|------|
| 点击"开始" | 倒计时从 25:00 开始，按钮变为"暂停" |
| 点击"暂停" | 倒计时暂停，按钮变为"继续" |
| 点击"继续" | 倒计时恢复 |
| 倒计时归零 | 自动弹出全屏提醒 + 播放音效 |
| 全屏提醒中按任意键/点击 | 退出提醒，回到初始状态（25:00） |

### 计时精度

使用 `setInterval` + `Date.now()` 差值计算，避免浏览器节流导致的计时漂移。

### 音效方案

Web Audio API 生成简单提示音（如 880Hz 正弦波，持续 0.5s，重复 3 次），无需加载外部音频文件。

## 技术方案

| 项目 | 选择 | 理由 |
|------|------|------|
| 技术栈 | vanilla HTML/CSS/JS | 极简，无构建依赖 |
| 全屏实现 | CSS fixed 覆盖层 | 无权限问题，100% 可靠 |
| 音效 | Web Audio API | 无需外部文件 |
| 存储 | 无 | 最简版不需要记录 |
| 文件结构 | 单 index.html | 双击即用 |

## 范围边界

**不包含（本期不做）：**
- 自定义时长（固定 25 分钟）
- 休息倒计时（仅工作倒计时）
- 历史记录 / 统计
- 多轮自动循环
- 通知 API（仅页面内提醒）
- 暗色/亮色主题切换

## 验收标准

- [ ] 双击 index.html 可在浏览器中正常打开
- [ ] 点击开始后倒计时准确运行
- [ ] 暂停/继续功能正常
- [ ] 倒计时归零后全屏覆盖层立即出现
- [ ] 提示音正常播放
- [ ] 按任意键可退出全屏提醒
- [ ] 退出后回到初始 25:00 状态
